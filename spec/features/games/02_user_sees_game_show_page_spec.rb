require 'rails_helper'

feature 'user sees game show page' do
  pending scenario 'user sees winner' do
    dave = FactoryGirl.create(:player)

  end
  pending scenario 'user sees each player with faction' do
  end
  pending scenario 'user sees each player with lord' do
  end
end
