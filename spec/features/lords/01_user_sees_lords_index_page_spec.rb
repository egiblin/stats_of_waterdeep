require 'rails_helper'

feature 'user sees lords index' do
  pending scenario 'user sees all lords' do
    dave = FactoryGirl.create(:player)

  end
  pending scenario 'user sees lords leaderboard' do
  end
end
