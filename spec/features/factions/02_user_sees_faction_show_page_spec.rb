require 'rails_helper'

feature 'user sees faction show page' do
  pending scenario 'user sees win/loss of faction' do
    dave = FactoryGirl.create(:player)

  end
  pending scenario 'user sees last player to use faction' do
  end
  pending scenario 'user sees player with best record with faction' do
  end
  pending scenario 'user sees player with worst record with faction' do
  end
end
